//arr * arr, 
function arrTimesArr(x, y){
    var res = [];
    x = [i];
    y = [j];
    for (var i = 0; i <= x.length; i++) {
    	for (var j = 0; j < y.length; j++) {
    		Things[i]
    	}
    	x[i]*y[j];
    	res.push(x[i]*y[j]);
    }
    return res;
}
console.log(arrTimesArr(2, 2));